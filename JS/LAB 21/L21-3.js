const numbers = [2, 4, 6, 8];
const allEven = numbers.every(element => element % 2 === 0);
console.log(allEven); 

const numbers2 = [1, 3, 5, 7];
const allOdd = numbers2.every(element => element % 2 !== 0);
console.log(allOdd); 